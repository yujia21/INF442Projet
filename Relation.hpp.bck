#pragma once

#include <string>
#include <vector>

class Relation {

public: 

   Relation(char * infile);
   ~Relation();
   
   void printdata();
   
   void write(char* outfile, std::vector<int> r);
   
private:

   std::vector<std::vector<int> > relations;
   
};
